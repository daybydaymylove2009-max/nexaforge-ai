# 智核万炼 NexaForge AI - 依赖配置
# 安装命令: pip install -r requirements.txt

# 核心依赖
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0

# 微调训练
peft>=0.6.0
trl>=0.7.0
bitsandbytes>=0.41.0

# 系统监控
psutil>=5.9.0

# 数据处理
numpy>=1.24.0
pandas>=2.0.0

# 工具库
tqdm>=4.65.0
safetensors>=0.4.0
huggingface-hub>=0.19.0

# 可选加速 (根据硬件自动选择)
# intel-extension-for-pytorch  # Intel CPU/GPU加速
# xformers  # 显存优化 (NVIDIA GPU)
